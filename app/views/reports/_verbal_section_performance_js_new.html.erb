<script type="text/javascript">
  var container = $("#verbal-section-performance-graph")[0];
  var dataset = [{name: "Percentile", data: [{y: <%= @paper.percentile %>, color: bar_colors[1][0]}], pointWidth: 40, color: "#800d92"}];
  new Highcharts.Chart({
    chart: {
        renderTo: container,
        type: 'bar',
        marginLeft: 100,
        backgroundColor: "#f1f1f1"
    },
    title: {
        text: "",
        align: 'left',
        x: 90
    },
    plotOptions: {
        line: {
            dataLabels: {
                enabled: true
            }
        }
    },
    credits: {
        enabled: false
    },
    xAxis: {
        lineColor: '#cecece',
        categories: ['Scaled Score'],
        labels: {
          style: {
            color: '#000',
            font: '11px LatoWebBlack, sans-serif'
          }
        }
    },
    yAxis: {
        lineColor: '#cecece',
        lineWidth: 1,
        allowDecimals: false,
        labels: {
          style: {
            color: '#000',
            font: '11px LatoWebBlack, sans-serif'
          }
        },
        title: {
            text: null
        },
        gridLineColor: '#cecece',
        min: 0,
        max: 100
    },
    plotOptions: {
        bar:{
            color: "#aaa"
        }
    },
    legend: {
        enabled: false
    },
    series: dataset
  })
</script>