<script type="text/javascript">
  var container = $("#verbal-section-performance-graph")[0];
  var dataset = [{name: "Percentile", data: [{y: <%= @paper.percentile %>, color: bar_colors[1][0]}], pointWidth: bar_width, color: "#800d92"}];
  new Highcharts.Chart({
    chart: {
        renderTo: container,
        type: 'bar',
        marginLeft: 10,
        backgroundColor: chart_background
    },
    title: {
        text: "",
        align: 'left',
        x: 90
    },
    credits: {
        enabled: false
    },
    xAxis: {
        lineColor: '#cecece',
        categories: [''],
        labels: {
          style: {
            color: '#000',
            font: '11px LatoWebBlack, sans-serif'
          }
        }
    },
    yAxis: {
        lineColor: '#cecece',
        lineWidth: 1,
        allowDecimals: false,
        labels: {
          style: {
            color: '#000',
            font: '11px LatoWebBlack, sans-serif'
          }
        },
        title: {
            text: "Percentile"
        },
        gridLineColor: '#cecece',
        min: 0,
        max: 100
    },
    plotOptions: {
        series: {
            pointWidth: bar_width
        },
        bar: {
            color: "#aaa",
            stacking: 'normal',
            dataLabels: {
                enabled: true,
                color: "#000",
                style: {
                    fontSize: label_inner_size
                }
            }
        },
        line: {
            dataLabels: {
                enabled: true
            }
        }
    },
    legend: {
        enabled: false
    },
    series: dataset
  })
</script>
